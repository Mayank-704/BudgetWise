// Navbar.tsx
// Top navigation for tracking pages
import React from "react";

const Navbar = () => {
  // TODO: Tab navigation for tracking pages
  return (
    <nav className="flex items-center gap-4 border-b bg-background p-2">
      {/* Tab buttons for each tracking page */}
      <div className="text-muted-foreground">Navbar Placeholder</div>
    </nav>
  );
};

export default Navbar;
